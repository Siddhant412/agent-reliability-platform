# `infra/docker`

Local Docker Compose and service configuration for Postgres, Redis, Temporal,
MinIO, OpenTelemetry, API, worker, and web.

Compose files should make local demos seedable and keep secrets outside logs
and trace payloads.
