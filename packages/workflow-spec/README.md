# `packages/workflow-spec`

Workflow DSL schema, parser contract, and canonical examples.

The first implementation should include the support-ticket-resolution workflow
from the product spec and validate that published workflow versions are
schema-safe and immutable.

Files:

- Schema: `schema/workflow.schema.json`
- Canonical example: `examples/support-ticket-resolution.v1.yaml`
